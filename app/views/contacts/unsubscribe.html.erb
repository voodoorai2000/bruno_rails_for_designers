<%= form_for(@contact) do |f| %>

  <div class="field">
    <%= f.label :newsletter %><br/>
    <%= f.check_box :newsletter, :checked => true  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
